// Operadores

Integer sum = 5 + 3;
Integer difference = 10 - 2;
Integer product = 4 * 7;
Double quotient = 20 / 4.0;

// Operadores comparacion

Boolean isEqual = (5 == 5); // true
Boolean isGreater = (10 > 5); // true
Boolean isNotEqual = (5 != 3); // true

// Operadores lógicos

Boolean result = (5 > 3) && (10 < 20); // true
Boolean result2 = (5 > 3) || (10 > 20); // true