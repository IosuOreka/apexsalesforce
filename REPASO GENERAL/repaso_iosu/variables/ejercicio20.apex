public class ejercicio{
    public static void solucion(){
        String estado = 'Activo';
        Boolean esActivo;
        if(estado == 'Activo'){
            esActivo = (estado == 'Activo');
        }
        System.debug('El estado del usuario es activo: ' + esActivo);
    }
}