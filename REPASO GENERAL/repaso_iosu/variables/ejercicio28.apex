public class ejercicio28 {
    public static void solucion() {
        String nombreCompleto = 'Juan Pérez';
        String primerNombre = nombreCompleto.substring(0, nombreCompleto.indexOf(' '));
        System.debug(primerNombre);
    }
}
